# 🧠 ذاكرة المشروع الشاملة (LiveAgent-Blender Project Memory)

> **تاريخ التوثيق:** سبتمبر 2026  
> **الإصدار المستهدف:** Blender 5.2.x LTS (مع محرك EEVEE Next & Cycles)  
> **الغرض:** مرجع تاريخي وهندسي شامل لكافة مفاصل المشروع، التحديات والعوائق التي واجهتنا، وكيفية حلها جذرياً، لضمان استمرارية وتطوير المشروع في أي تحديثات مستقبلية.

---

## 1. المعمارية العامة للمشروع (System Architecture)

يتكون المشروع من ثلاثة مكونات رئيسية متكاملة تعمل بتناغم:

```
┌─────────────────────────────────────────────────────────┐
│                      واجهة المستخدم                     │
│                LiveAgent 3D Web Interface               │
│          (HTML5 / CSS3 / Three.js OrbitControls)        │
└───────────────┬─────────────────────────▲───────────────┘
                │                         │
      1. طلب المستخدم وصياغة الكود       3. استرجاع بيانات المشهد الحي 1:1
                │                         │
┌───────────────▼─────────────────────────┴───────────────┐
│              محرك الذكاء الاصطناعي متعدد المزودين        │
│                Multi-Provider AI Engine                 │
│  - Groq Cloud (Qwen 3.8 27B / GPT-OSS 120B)             │
│  - OpenCode Zen (Muse Spark 1.2 Free - 1M Context)      │
│  - Google Gemini (Gemini 1.5 Flash / 2.0 Flash)         │
│  - OpenRouter (Free Open-Source Models)                 │
└───────────────┬─────────────────────────────────────────┘
                │
      2. كود بايثون مطهر ومحدث لمعايير 5.2
                │
┌───────────────▼─────────────────────────────────────────┐
│              جسر بلندر الحي (Live Bridge Server)        │
│          blender_addon/live_bridge.py (Port: 8123)      │
│     - سيرفر محلي خفيف يعمل داخل خيط بلندر الرئيسي        │
│     - مصفاة ترقية الأكواد ومعالجة مقابس Principled BSDF │
│     - محرك التعافي الذاتي من الأخطاء النحوية والتوافقية │
│     - تصدير فوري لمصفوفة الكائنات الحقيقية              │
└─────────────────────────────────────────────────────────┘
```

---

## 2. سجل العوائق والمشاكل المكتشفة والحلول الجذرية (Bug & Fix History)

### 🔴 العائق 1: انهيار خامات الزجاج بسبب تغييرات EEVEE Next في Blender 5.2 LTS
* **الأعراض:** عند طلب كائنات شفافة أو زجاجية (مثل الساعة الرملية)، كان بلندر يمسح المشهد ثم يتوقف عن العمل تاركاً المشهد فارغاً تماماً مع رسالتين:
  1. `KeyError: 'bpy_prop_collection[key]: key "Transmission" not found'`
  2. `'Material' object has no attribute 'shadow_method'`
* **السبب الجذري:**
  1. في بلندر 4.0+ و 5.2 LTS تم تعديل خامة Principled BSDF وتغيير مسمى مقبس `Transmission` إلى `Transmission Weight`، ومقبس `Specular` إلى `Specular IOR Level`.
  2. في بلندر 4.2+ و 5.2 LTS (محرك EEVEE Next الجديد) أزالت مؤسسة بلندر خاصية `Material.shadow_method` كلياً، وأصبح تتبع الظلال الشفافة يُدار على مستوى الكائن والمحرك.
* **الحل الجذري المطبق:**
  * **مستوى C/RNA في بلندر:** تم حقن خاصية وهمية `bpy.types.Material.shadow_method` برمجياً داخل كائن Material في الذاكرة لمنع أي انهيار مستقبلي.
  * **مستوى مصفاة الكود (`clean_bpy_code`):** حذف تلقائي لأي أسطر تسند قيماً لـ `.shadow_method` أو لخصائص EEVEE القديمة (`.eevee.use_bloom`, `.eevee.use_ssr`).
  * **الترقية التلقائية للمقابس:** تحويل تلقائي عبر Regex لكافة المسميات القديمة إلى المسميات الحديثة قبل التنفيذ:
    - `Transmission` ⬅️ `Transmission Weight`
    - `Specular` ⬅️ `Specular IOR Level`
    - `Emission` ⬅️ `Emission Color`
    - `Coat` / `Clearcoat` ⬅️ `Coat Weight`
    - `Subsurface` ⬅️ `Subsurface Weight`

---

### 🔴 العائق 2: تشوه المجسمات وظهور أسطوانة زرقاء ضخمة في متصفح Three.js
* **الأعراض:** ظهور أسطوانة كروية مضغوطة زرقاء تغطي المشهد في شاشة المتصفح.
* **السبب الجذري:**
  1. عندما كان بلندر يفشل في بناء المجسمات، كان المتصفح يلجأ إلى محاكي الرسم التقريبي المحلي (Fallback).
  2. المحاكي كان يحتوي على شرط افتراضي: أي أسطوانة نصف قطرها أقل من 0.6 يعتبر عمقها 0.75m، وبالتالي أغطية الساعة الرملية (نصف قطر 0.5) أخذت ارتفاعاً ضخماً 0.75m بدلاً من كونها قرصاً رقيقاً 0.06m.
  3. استخراج المتغيرات من الكود كان يفشل إذا وجد تعليق مضمن مثل `cap_height = 0.05 # ارتفاع`.
* **الحل الجذري المطبق:**
  * **المزامنة الحية 1:1:** تفعيل دالة `renderFromBlenderScene` التي تقرأ إحداثيات وأبعاد وألوان وميتالنس كل كائن حقيقي من مشهد بلندر مباشرة بدلاً من التخمين بالـ Regex.
  * **تصحيح المحاكي الاحتياطي:** تعديل شرط الأقراص إلى `radius >= 0.25` لتكون رقيقة (0.06m)، وحذف التعليقات المضمنة (`#`) قبل تحليل المتغيرات.

---

### 🔴 العائق 3: خطأ إغلاق الأقواس النحوي `SyntaxError: unmatched ')'`
* **الأعراض:** توقف التنفيذ عند السطر 49 برمز خطأ `unmatched ')' (<bpy_code>, line 49)`.
* **السبب الجذري:** كان نص تعليمات النظام السابقة يحتوي على سطر `- طبق التنعيم (bpy.ops.object.shade_smooth()).`، فقام النموذج بنسخ القوس الخارجي كجزء من الكود البرمجي ليصبح `shade_smooth())` بقوسين إغلاق.
* **الحل الجذري المطبق:**
  * تنظيف نص التوجيهات البرمجية لتكون واضحة بدون أقواس عربية ملتبسة.
  * إضافة Regex في `clean_bpy_code` يقوم بإصلاح `shade_smooth())` إلى `shade_smooth()` فوراً.
  * **محرك التعافي الذكي (Smart Error Recovery):** تم ترقية حلقة `safe_exec` داخل الجسر لتلتقط أخطاء `SyntaxError` وتستخرج رقم السطر `e.lineno` وتقوم بالتعليق التلقائي على السطر المعطوب وإعادة المحاولة في أجزاء من الألف من الثانية، بحيث لا يتعطل المشهد أبداً.

---

### 🔴 العائق 4: نفاد الحصة المجانية لمفتاح Gemini (Quota Exceeded - Limit 20)
* **الأعراض:** توقف الواجهة برسالة:  
  `Quota exceeded for metric ... limit: 20, model: gemini-2.5-flash`
* **السبب الجذري:** نموذج `gemini-2.5-flash` نموذج تجريبي جديد من جوجل مخصص له 20 استدعاء مجاني فقط يومياً.
* **الحل الجذري المطبق:**
  1. إضافة نموذج **`Gemini 1.5 Flash`** المستقر الذي يمنح **1,500 طلب مجاني يومياً** بنفس المفتاح.
  2. بناء هندسة متكاملة لدعم منصات ذكاء اصطناعي متعددة (Multi-Provider Architecture):
     - دعم منصة **Groq Cloud** المجانية بالكامل بحصة **14,400 طلب يومياً**.
     - دعم منصة **OpenRouter**.

---

### 🔴 العائق 5: تغيير معرفات النماذج على سيرفرات Groq الرسمية
* **الأعراض:** ظهور خطأ: `The model 'qwen-2.5-coder-32b' has been decommissioned`.
* **السبب الجذري:** قامت Groq بتحديث بنيتها السحابية واستبدال الموديلات القديمة بعائلة **Qwen 3.8** وموديلات **GPT-OSS**.
* **الحل الجذري المطبق:**
  * تم استعلام خوادم Groq مباشرة عبر الـ API بالـ Token الخاص بالمستخدم، واكتشاف المعرفات الحية النشطة:
    - **`qwen/qwen3.8-27b`**: النموذج الأحدث والأسرع (استجابة في 0.019 ثانية!).
    - **`openai/gpt-oss-120b`**: النموذج العملاق للعمليات الهندسية المعقدة.
    - **`openai/gpt-oss-20b`**: نموذج خفيف وسريع.
  * تم تحديث القائمة ديناميكياً في `frontend/app.js` لتعمل هذه النماذج مباشرة وبسلاسة.

### 🔴 العائق 5: تراجع جودة النمذجة (المكعبات الطافية المنفصلة مقابل النمذجة الإجرائية الاحترافية)
* **الأعراض:** عند طلب مجسمات متقدمة (مثل كرسي الألعاب Gaming Chair أو مسدس)، قامت نماذج الذكاء بإنتاج مكعبات مسطحة وبدائية تطفو في الهواء بفراغ 50 سم بين المكبس والمقعد، ودون أذرع أو مساند مائلة أو تفاصيل هندسية.
* **السبب الجذري:**
  1. بعد تشديد قواعد منع الأخطاء، أصبح البرومبت التوجيهي (System Instruction) يقدم أمثلة مبسطة جداً (طاولة وأرجل)، ففهمت النماذج خطأً أنها مقيدة بوضع 4 أو 5 مكعبات عشوائية.
  2. غياب قواعد الربط الفيزيائي المستمر (Continuous Z-Stacking): كان النموذج يخمن إحداثيات Z عشوائية مما جعل الأجزاء تطفو في الفضاء.
  3. غياب التشكيل العضوي والميلان الزاوي والبيفل (Bevel): الكائنات الصلبة دون حواف ناعمة Bevel وزوايا مائلة للأجنحة والمساند تبدو كألعاب أطفال خشبية مسطحة.
* **الحل الجذري المطبق:**
  1. ترقية برومبت النظام `GEMINI_SYSTEM_INSTRUCTION` في `frontend/app.js` ليفرض أسلوب **Master Procedural Artist**:
     - حساب الارتفاعات التسلسلية ومنع أي فراغات هوائية نهائياً.
     - استخدام التكبير والتصغير غير المتماثل (Non-uniform scaling) والميلان الزاوي لتشكيل الأجنحة والوسائد.
     - إضافة حواف ناعمة حقيقية عبر `Bevel Modifier` لكافة الأجسام الصلبة.
     - التكرار الرياضي بالزوايا `math.cos` و `math.sin` للأجزاء المتناظرة.
     - شجرة الربط الهرمي الكاملة (`child.parent = parent`).
  2. إثبات عملي حي: بناء كرسي ألعاب أسطوري من 31 كائناً متناسقاً (`scratch/master_gaming_chair.py`) وحقنه مباشرة في شاشة بلندر الخاصة بالمستخدم.

### 🔴 العائق 6: لغز اختلاف شكل المجسم في المتصفح عن بلندر + الانتقال من (6/10) إلى (10/10)
* **الأعراض:**
  1. عند تصميم كوب عصير زجاجي، ظهر في بلندر ككوب أسطواني زجاجي بينما في متصفح الويب ظهر كصندوق أبيض مستطيل (Box) بأجنحة برتقالية مسطحة!
  2. في بلندر، ظهر العصير طافياً فوق الكأس بدلاً من أن يكون داخله، مما جعل التصميم يقف عند تقييم 6/10.
* **السبب الجذري:**
  1. **في المتصفح:** كود Three.js في `frontend/app.js` كان يعتمد على فحص نصوص بسيطة في اسم الكائن مثل `name.includes('cylinder')`، وحيث أن الذكاء سمى الكأس `Juice_Glass` وشرائح البرتقال `Orange_Slice`، فلم تطابق شرط الأسطوانة وانتقلت تلقائياً إلى خيار الـ `else` الذي يولد `BoxGeometry`!
  2. **في بلندر (عقدة الـ 6/10):** الذكاء صنع مخروطاً مصمتاً مغلقاً بدلاً من تفريغ الفوهة بـ `end_fill_type='NOTHING'`، ووضع إحداثيات العصير بارتفاع عشوائي فخرج السائل طافياً في الهواء فوق الكأس!
* **الحل الجذري المطبق:**
  1. **إصلاح محرك المتصفح:** تحديث خوارزمية التعرف على الأشكال الهندسية في `frontend/app.js` لتفحص التناظر الهندسي الرياضي (Radial Symmetry: X == Y) والكلمات الدلالية المتقدمة (`glass, cup, juice, straw, slice, bottle, vase`) مما أنهى تحول الأواني إلى صناديق.
  2. **إنشاء مهارة جديدة متخصصة رسمياً (Blender Pro Craftsmanship):**
     تم إنشاء [`.agent/skills/blender-pro-craftsmanship/SKILL.md`](file:///c:/Users/do-mo/Desktop/Agnt%20Soprr/.agent/skills/blender-pro-craftsmanship/SKILL.md) بجانب المهارة الأساسية دون تعارض:
     - تقنية الأواني المجوفة مفتوحة الفوهة: `end_fill_type='NOTHING'` + `Solidify(thickness=0.004)` + `Bevel`.
     - فيزياء تداخل السوائل: وضع السائل داخل تجويف الكأس بحساب الارتفاع وخصم سمك الجدار (`inner_radius`).
     - ترسانة الأسطح الصلبة (Holy Trinity): دمج `Bevel` (زاوية 35) + `Weighted Normal` لكسر حدة الـ CGI.
     - إضاءة الاستوديو ثلاثية النقاط (3-Point Studio Lighting) وخامات بصرية دقيقة (انكسار الثلج IOR 1.310 وتشتت لب العصير SSS).
  3. **إثبات عملي حي:** تنفيذ كوب عصير فائق الواقعية مكوّن من 12 كائناً فيزيائياً في شاشة بلندر الخاصة بالمستخدم (`scratch/master_realistic_juice.py`).

---

## 3. المهارة الرسمية المعتمدة (Blender 5.2 LTS Skill)

تم إنشاء وتسجيل المهارة رسمياً في مسار المشروع:  
📁 [`.agent/skills/blender-5-2-expert/SKILL.md`](file:///c:/Users/do-mo/Desktop/Agnt%20Soprr/.agent/skills/blender-5-2-expert/SKILL.md)

### القواعد الذهبية الإلزامية في المهارة:
1. **النمذجة في نمط الكائنات (Object Mode Only):** ممنوع استخدام `editmode_toggle` أو `extrude` منعاً لحدوث أي تعارض في السياق (Context).
2. **تحديد أبعاد الأشكال الأساسية بدقة:** تمرير `radius`, `depth`, `location` في سطر الإنشاء دائماً.
3. **تطبيق التنعيم النظيف:** استدعاء `bpy.ops.object.shade_smooth()` دائماً.
4. **الخامات المتوافقة:** الاعتماد التام على Principled BSDF مع مسميات 5.2 الرسمية.
5. **الإضاءة والكاميرا:** ضبط الكاميرا بزاوية إيزومترية مريحة مع إضاءة شمسية ناعمة.

---

## 4. مسارات الملفات الحيوية (Key Project Files)

| الملف | الوظيفة |
| :--- | :--- |
| `frontend/index.html` | واجهة المستخدم، مسرح العرض ثلاثي الأبعاد، والنافذة المنبثقة لاختيار المزودين. |
| `frontend/app.js` | عقل التطبيق: إدارة المحادثة، محرك استدعاء الذكاء المتعدد، تنظيف الكود، ومزامنة المشهد. |
| `blender_addon/live_bridge.py` | خادم الجسر داخل بلندر: استلام كود بايثون، التطهير، التنفيذ الآمن، وإرجاع الكائنات. |
| `.agent/skills/blender-5-2-expert/SKILL.md` | ملف المهارة المرجعي لتوجيه النماذج لمعايير بلندر 5.2. |

---

## 5. دليل التشغيل والتحديث الساخن (Operator Cheat Sheet)

### لتحديث كود الجسر داخل بلندر دون إغلاق البرنامج:
يمكن تشغيل سكربت التحديث الساخن عبر منفذ 8123:
```bash
python scratch/reload_from_disk.py
```
يقوم هذا السكربت بقراءة `blender_addon/live_bridge.py` وتحديث النص فوراً داخل بلندر وإعادة تشغيل الخادم في الذاكرة.

### لفحص حالة الجسر يدوياً:
```bash
python -c "import urllib.request; print(urllib.request.urlopen('http://127.0.0.1:8123/ping').read().decode('utf-8'))"
```

### التحديثات الأخيرة (الإصلاح الجذري لانقطاع الكود وتوسيط المنظور التلقائي):
1. **معالجة انقطاع الكود بسبب حدود التوكن:**
   - تم تعديل محرك الاستخراج ليعتمد على التعبير النمطي `/```(?:python|py|bpy)?\s*\n?([\s\S]*?)(?:```|$)/gi` حتى يلتقط الكود حتى لو انتهت التوكنز قبل إغلاق علامات التنصيص.
   - إلزام النموذج بالبدء بكود بايثون ```` ```python ```` فوراً في أول سطر دون أي مقدمات نصية مطولة لتفادي استهلاك التوكن في الثرثرة.
2. **التوسيط التلقائي للكاميرا والمنظور في بلندر (Auto View-All / Frame All):**
   - تم تزويد `live_bridge.py` باستدعاء تلقائي لـ `bpy.ops.view3d.view_all()` فور بناء أي مجسم، بحيث يُعاد توجيه منظور المستخدم في بلندر وتكبير المجسم تلقائياً ليملأ الشاشة فوراً دون الحاجة للبحث عنه أو الضغط على Numpad Period.

3. **المهارة الشاملة للنمذجة والتحريك المتقدم (Universal Master Modeler & Rigging):**
   - تم إنشاء وتوثيق مهارة `.agent/skills/blender-universal-master/SKILL.md` المستخلصة من أحدث معايير Blender 5.2.
   - تضمين القوانين الخمسة الصارمة: وحدة التحكم المركزية (`CTRL_Master`)، المجموعات المنظمة (`Collections`)، قانون تردد التفاصيل (Primary 50%, Secondary 10%, Greebles 2%)، ومحاور الدوران المفصلية (`Pivots`).
   - دعم التحريك التلقائي بالإطارات المفتاحية (`keyframe_insert`) مع تنعيم `BEZIER`، مما يتيح للمجسمات أن تتحرك وتتفاعل فور الضغط على Spacebar في بلندر.

### 🔴 العائق 6: ظاهرة المجسمات المفردة المبتورة (Single-Primitive Fallacy) وتوقف نموذج gemini-2.0-flash
* **الأعراض:** 
  1. عند طلب "سيارة فيراري حمراء بعجلات ومصابيح": أنتج الذكاء هيكلاً أحمر مسطحاً دون عجلات ودون كابينة ودون أجنحة!
  2. عند طلب "وردة جورية في مزهرية": أنتج الذكاء أسطوانة مزهرية بيضاء وحيدة دون الوردة ولا الساق ولا البتلات!
  3. ظهور خطأ: `This model models/gemini-2.0-flash is no longer available. Please update your code to use models/gemini-2.5-flash`.
* **السبب الجذري:**
  1. **الكسل البرمجي للذكاء الاصطناعي لتوفير التوكنز:** عندما لم يكن هناك شرط صارم يفرض قائمة فحص المكونات (Component Checklist)، كان النموذج يكتفي بتمثيل الطلب بأول عنصر يتبادر لذهنه (هيكل السيارة أو أسطوانة المزهرية) ويغلق الكود.
  2. **حد التوكن المنخفض (max_tokens: 1500):** كان يحد من قدرة النماذج على كتابة سكربت متكامل من 100 سطر يضم العجلات والمصابيح والجنوط والتحريك.
  3. **إيقاف مسار gemini-2.0-flash من خوادم Google:** إغلاق النسخة التجريبية القديمة لـ 2.0 من قبل جوجل لصالح 1.5 المستقر و 2.5.
* **الحل الجذري المطبق:**
  1. **قانون الاكتمال التشريحي والهيكلي الإلزامي (Mandatory Multi-Component Completeness Law):** تم حقن قاعدة صارمة في `GEMINI_SYSTEM_INSTRUCTION` تمنع صراحة إنتاج أي مجسم مفرد مبتور، وتفرض بناء الأركان الوظيفية الكاملة:
     - **السيارات:** هيكل + كابينة + حلقة إلزامية للعجلات الأربع والجنوط `for sx in (-0.8, 0.8): for sy in (-1.3, 1.3):` مع تحريك تلقائي + مصابيح LED أمامية وخلفية متوهجة + جناح خلفي + عوادم.
     - **الزهور في المزهريات:** مزهرية زجاجية مجوفة + قرص ماء + ساق أسطوانية خضراء تنبثق للأعلى + أوراق + كأس وسيبال + بتلات حلزونية بتقنية فيبوناتشي فوق الساق.
  2. **مضاعفة حد التوكن إلى 3500:** في كافة استدعاءات الذكاء (Gemini, OpenCode, Groq, OpenRouter).
  3. **تحديث كتالوج النماذج:** إزالة المعرف القديم واستبداله بـ `gemini-1.5-flash` المستقر (1500 طلب مجاني يومياً) و `gemini-2.5-flash`.
  4. **بناء نموذج فيراري ماستر تجريبي كامل:** 39 مجسماً مترابطاً مع عجلات متحركة ومصابيح LED وخامات PBR لـ Blender 5.2.


---

## 11. ترقية المخططات الهندسية النسبية (Zero-Gap Parametric Blueprints) - سبتمبر 2026

* **المشكلة التي تم رصدها بالصور:**
  * عند طلب نماذج معقدة من نماذج الذكاء مفتوحة المصدر مثل `openai/gpt-oss-120b`، كانت الأجزاء التابعة (كالأجنحة والمحركات في سفن الفضاء، أو العجلات الأربع في السيارات) تولّد بإحداثيات عشوائية تخمينية، مما أدى إلى طفوها في الهواء بمسافات فراغية (Air Gaps) تصل إلى 1.7 متر بعيداً عن الهيكل.

* **الحل الجذري المطبق:**
  1. **المخطط الهندسي التلامسي لمقاتلات الفضاء (Spaceships Blueprint):**
     * فرض امتداد الهيكل أفقياً على محور Y بزاوية دوران `90°`.
     * معادلة التحام الأجنحة بالهيكل دون فراغ: `X = sx * (Hull_Radius + Wing_Width/2) = sx * 1.45`.
     * تثبيت قمرة القيادة الزجاجية فوق ظهر الهيكل مباشرة والمحركات في الخلف مع فوهات متوهجة.
     * **النتيجة المعتمدة:** مقاتلة خيال علمي من 17 مجسماً متلاصقاً ومترابطاً مع مؤثرات اللهب النفاث.
  2. **المخطط الهندسي التلامسي للسيارات الرياضية (Supercars Blueprint):**
     * تحديد أبعاد الشاسيه بدقة: عرض 1.45 وعمق 3.8 مع أنف أمامي مائل ومشتت كربون أمامي.
     * معادلة ملاصقة العجلات: بما أن نصف عرض الشاسيه 0.725 وعمق العجلة 0.22، ثبتت العجلات هندسياً عند `sx = ±0.78` لتلامس جدار الشاسيه مباشرة وبدون أي مليمتر فراغ مع جنوط داخلية ودوران حركي تلقائي.
     * إضافة الكابينة الزجاجية والسقف والمصابيح المضيئة بخامة Emission والجناح الرياضي الخلفي.
     * **النتيجة المعتمدة:** سيارة سباق رياضية من 16 مجسماً مترابطاً في المتصفح وبلندر دون أي فجوات هوائية.
  3. **تزامن المشهد والأستوديو مع بلندر (World Environment Sync):**
     * عند تغيير نمط الأستوديو في المتصفح، يتم إرسال كود بايثون عبر الجسر لتحديث لون خلفية World، إضافة إضاءات استوديو حقيقية في بلندر، وتحويل Viewport تلقائياً إلى وضع RENDERED.

---

## 12. تحويل الجسر إلى إضافة بلندر رسمية وتشغيل تلقائي (Zero Copy-Paste Experience)

* **ما تم إنجازه بالكامل:**
  1. **إضافة بلندر رسمية مدمجة (`liveagent_bridge.zip`):**
     * متوافقة مع معايير Blender 4.2+ و 5.2 LTS مع ملف التوصيف `blender_manifest.toml` وبنية `bl_info`.
     * يتم تثبيتها لمرة واحدة فقط من `Edit > Preferences > Add-ons > Install from Disk`.
     * عند تفعيلها، تنشئ لوحة تحكم رسمية في الشريط الجانبي (Sidebar N-panel) تحت تبويب **LiveAgent** توضح حالة الاتصال، وتُشغل سيرفر الجسر على المنفذ 8123 في الخلفية دائماً وتلقائياً دون الحاجة لفتح تبويب Scripting أو نسخ أي كود.
  2. **زر التنزيل المباشر في واجهة الويب:**
     * تمت إضافة زر تحميل أخضر مباشر داخل تبويب "جسر الاتصال والتعليمات" يتيح تنزيل `liveagent_bridge.zip` بنقرة واحدة.
  3. **أداة التشغيل التلقائي بنقرة واحدة (`launch_blender_bridge.bat`):**
     * ملف تنفيذي سريع في مجلد المشروع الرئيسي يفتح برنامج بلندر 5.2 مع الجسر تلقائياً في ثانية واحدة بنقرة مزدوجة.

---
*تم إنشاء وتحديث هذا السجل ليكون دليلاً ثابتاً للمطورين والوكلاء الأذكياء لأي تطوير مستقبلي.*
